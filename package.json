{
  "name": "react-test",
  "version": "0.0.0",
  "description": "Demo/test project",
  "main": "index.js",
  "scripts": {
    "build": "rm -f build/* && mkdir -p build && cp index.html build && node_modules/.bin/webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Rosuav/react-test.git"
  },
  "author": "Chris Angelico",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Rosuav/react-test/issues"
  },
  "devDependencies": {
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "jquery": "^3.1.0"
  }
}
